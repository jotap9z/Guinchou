package com.guinchou.app.ui.theme

import androidx.compose.ui.graphics.Color

// Verde-limão principal da identidade do Guinchou.
val GuinchouGreen = Color(0xFFB7E500)

// Variação mais clara usada em elementos destacados.
val GuinchouGreenLight = Color(0xFFD2F34A)

// Variação mais escura do verde.
val GuinchouGreenDark = Color(0xFF8DB300)

// Fundo principal extremamente escuro.
val GuinchouBackground = Color(0xFF07151B)

// Fundo secundário usado em cards e campos.
val GuinchouSurface = Color(0xFF0D2028)

// Fundo de componentes um pouco mais claros.
val GuinchouSurfaceVariant = Color(0xFF132B34)

// Branco principal.
val GuinchouWhite = Color(0xFFF7FAFB)

// Texto secundário.
val GuinchouGray = Color(0xFF9EADB3)

// Bordas dos componentes.
val GuinchouBorder = Color(0xFF294049)

// Cor utilizada para indicar erro.
val GuinchouError = Color(0xFFFF5C5C)

// Cor utilizada para estados positivos.
val GuinchouSuccess = Color(0xFF49D17D)